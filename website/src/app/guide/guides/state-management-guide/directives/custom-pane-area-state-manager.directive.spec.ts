import {CustomPaneAreaStateManagerDirective} from './custom-pane-area-state-manager.directive';

describe('CustomPaneAreaStateManagerDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomPaneAreaStateManagerDirective();
    expect(directive).toBeTruthy();
  });
});
