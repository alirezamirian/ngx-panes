import {FullHeightDirective} from './full-height.directive';

describe('FullHeightDirective', () => {
  it('should create an instance', () => {
    const directive = new FullHeightDirective();
    expect(directive).toBeTruthy();
  });
});
